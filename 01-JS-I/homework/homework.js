// En estas primeras 6 preguntas, reemplaza `null` por la respuesta

//ESTUDIANTE:  adalsus
// Crea una variable "string", puede contener lo que quieras:
const nuevaString = 'Híbrido';

// Crea una variable numérica, puede ser cualquier número:
const nuevoNum = 45.06;

// Crea una variable booleana:
const nuevoBool = true;

// Resuelve el siguiente problema matemático:
// ¿Qué valor pondrías en vez de null para que la expresión se evalue a verdadera?
const nuevaResta = 10 - /*null le reemplazo por*/ 5 === 5;

// Resuelve el siguiente problema matemático:
// ¿Qué valor pondrías en vez de null para que la expresión se evalue a verdadera?
const nuevaMultiplicacion = 10 * /*null le reemplazo por */ 4 === 40 ;

// Resuelve el siguiente problema matemático:
// ¿Qué valor pondrías en vez de null para que la expresión se evalue a verdadera?
const nuevoModulo = 21 % 5 === /*null le reemplazo por */ 1;
//ESTUDIANTE:  adalsus

// No modificar nada debajo de esta línea
// --------------------------------

module.exports = {
  nuevaString,
  nuevoNum,
  nuevoBool,
  nuevaResta,
  nuevaMultiplicacion,
  nuevoModulo,
};